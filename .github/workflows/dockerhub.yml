name: Build and Push to Docker Hub

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push to Docker Hub
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./docker/Dockerfile
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/argosbx:latest

    - name: Test the image
      run: |
        echo "🧪 测试拉取和运行..."
        docker run -d --name test-argosbx ${{ secrets.DOCKERHUB_USERNAME }}/argosbx:latest
        sleep 20
        echo "=== 容器日志 ==="
        docker logs test-argosbx --tail 20
        docker stop test-argosbx
        docker rm test-argosbx

    - name: Success message
      run: |
        echo "🎉 成功推送到 Docker Hub!"
        echo "🐳 镜像地址: ${{ secrets.DOCKERHUB_USERNAME }}/argosbx:latest"
        echo "🚀 使用命令: docker run -d ${{ secrets.DOCKERHUB_USERNAME }}/argosbx:latest"
        echo ""
        echo "📖 完整使用示例:"
        echo "docker pull ${{ secrets.DOCKERHUB_USERNAME }}/argosbx:latest"
        echo "docker run -d --name argosbx ${{ secrets.DOCKERHUB_USERNAME }}/argosbx:latest"
        echo "docker exec argosbx agsbx list"
